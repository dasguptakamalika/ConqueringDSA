class Solution {
    public boolean containsDuplicate(int[] nums) {
        HashSet<Integer> seen = new HashSet<>();
        
        for (int num : nums) {
            if (!seen.add(num)) {
                return true;
            }
        }
        return false;
    }
}
if (!seen.add(num))
   ↓
"If I CAN'T add this number (because it's already there)"
   ↓
"Then it's a DUPLICATE!"
The ! flips the meaning from "added successfully" to "already exists" 🎯
