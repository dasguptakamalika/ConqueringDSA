class Solution {
    public boolean isAnagram(String s, String t) {
        if (s.length() != t.length()) {
            return false;
        }
        
        int[] freq = new int[26];
        
        for (int i = 0; i < s.length(); i++) {
            freq[s.charAt(i) - 'a']++;
            freq[t.charAt(i) - 'a']--;           //consider the examples like car and cat , unless and until they are from from indices, cancellation wont work.
                                                 //count va;ues indicate something extra or less , hence we have the count
        }

                                                               /* for (int i = 0; i < freq.length; i++) {
                                                                    int count = freq[i];
                                                                    if (count != 0) {
                                                                        return false;
                                                                    }
                                                                     }                                                           
                                                                    freq[0]  = 0   // 'a'
                                                                    freq[2]  = 0   // 'c'
                                                                    freq[17] = +1  // 'r'
                                                                    freq[19] = -1  // 't'
                                                                    The for-each loop is simply checking each of these numbers.
                                                                    It doesn’t care which letter it is.
                                                                    It only asks:
                                                                    
                                                                    “Is this number zero or not?”
                                                                    Why this is enough
                                                                    0 → letter is balanced
                                                                    +1 → extra in s
                                                                    -1 → extra in t*/

        for (int count : freq) {                 
            if (count != 0) {
                return false;
            }
        }
        
        return true;
    }
}
